
# enabling OLED makes the right master super slow. Need to figure out if we can
# use the OLED on the slave.
# OLED_DRIVER_ENABLE=yes

# didn't have luck getting the device to show in `qmk console -l`
# CONSOLE_ENABLE=yes
